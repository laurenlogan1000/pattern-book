{
  "name": "pattern-book",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "db:init": "wrangler d1 execute pattern-book --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute pattern-book --local --file=schema.sql",
    "deploy": "wrangler pages deploy",
    "mock": "node scripts/mock-claude.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
